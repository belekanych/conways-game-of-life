/**
 * Determines how much cells will be generated randomly.
 * Possible values: from 0 to 1 (0 < x < 1).
 */
export const RAND_SIZE = 0.3

/**
 * Constant that is used to create (true) or delete (false) cells while drawing.
 */
export const MODE_PENCIL = true

/**
 * Constant that is used to create (true) or delete (false) cells while drawing.
 */
export const MODE_ERASER = false